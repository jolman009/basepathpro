# Coach Training App

This is a Vite + React application that provides a coaching dashboard for uploading, analyzing, and reviewing player videos.

## Requirements

- Node.js (version 18 or higher recommended)
- pnpm or npm for package management
- A Supabase project with tables and storage bucket as described in `/env.example`

## Setup

1. Install dependencies:
   ```bash
   pnpm install
